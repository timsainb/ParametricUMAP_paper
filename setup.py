from setuptools import find_packages, setup

setup(
    name='tfumap',
    packages=find_packages(),
    version='0.1.0',
    description='A UMAP extension to embed with neural networks',
    author='Tim Sainburg',
    license='MIT',
)
